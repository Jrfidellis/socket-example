Trabalho de Redes - Unisinos

Como rodar:

- Tenha uma versão atualizada do NodeJS instalada (testado com NodeJS v12);
- Abra um terminal na pasta raiz do projeto;
- Rode `npm install` e aguarde;
- Rode `npm start`;
- Abra `localhost:3000` no navegador (testado com Chrome).